uploadarchive.allowUnreachable::
	trueの場合、クライアントが `git archive --remote` を使用して、ref先端から到達可能かどうかに関係なく、任意のツリーを要求できるようにします。詳細については、 linkgit:git-upload-archive[1]の「SECURITY」セクションの説明を参照してください。デフォルトは `false` です。
