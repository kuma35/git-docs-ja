attr.tree::
	作業ツリーの +.gitattributes+ ファイルの代わりに、 属性(attributes)を読み取るためのリポジトリ内のツリーへの参照。 値が有効なツリー・オブジェクトに解決されない場合は、 代わりに空のツリーが使用されます。 GIT_ATTR_SOURCE 環境変数または `--attr-source` コマンド・ライン・オプションが使用されている場合、 この構成変数は効果がありません。
