sequence.editor::
	リベース命令ファイル(rebase instruction file)を編集するために `git rebase -i` によって使用されるテキスト・エディタ。 この値は、 使用時にシェルによって解釈されることを意図しています。 これは、 `GIT_SEQUENCE_EDITOR` 環境変数によってオーバーライドできます。構成されていない場合は、代わりにデフォルトのコミット・メッセージ・エディタが使用されます。
