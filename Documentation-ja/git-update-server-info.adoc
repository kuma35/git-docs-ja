git-update-server-info(1)
=========================

NAME
----
git-update-server-info - ダムサーバー(dumb servers)を支援するための補助情報ファイル(auxiliary info file)を更新する


SYNOPSIS
--------
[synopsis]
	'git update-server-info' [-f | --force]

DESCRIPTION
-----------
オンザフライ・パック生成(on-the-fly pack generations)を行わないダム・サーバー(dumb server)は、クライアントがサーバーの参照とパックを検出できるように、 $GIT_DIR/info と $GIT_OBJECT_DIRECTORY/info ディレクトリにいくつかの補助情報ファイル(auxiliary information files)を持っている必要があります。このコマンドは、そのような補助ファイルを生成します。

OPTIONS
-------
`-f`::
`--force`::
	情報ファイルをイチから作り直すことで更新します。

OUTPUT
------

現在、 このコマンドは以下のファイルを更新します。 それらが何のためにあるかについては、 linkgit:gitrepository-layout[5] を参照してください:

* objects/info/packs

* info/refs

GIT
---
Part of the linkgit:git[1] suite
